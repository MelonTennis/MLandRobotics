% run PLAL 20 times on serum data + new features using a PCA tree and 
% record the error and number of queries
n_runs = ;

error = zeros(n_runs,1);
nqueries = zeros(n_runs,1);
for i=1:n_runs
    % generate new data, add extra features, make a PCA tree, and run your 
    % PLAL code, and record the error and number of queries needed for each run
end

avg_nqueries_serum2PCA = ;
avg_error_serum2PCA = ;